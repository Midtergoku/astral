# Erros que eu ja cometi neste projeto

> A tabela completa. Os 5 mais caros ficaram inline no CLAUDE.md; estes sao todos.
>
> Extraido do `CLAUDE.md` em 01/08/2026, na reorganizacao em camadas.
> Nada foi apagado. O arquivo original inteiro esta em `historico/CLAUDE-original-2989.md`.
>
> Ordem do Lucas em 30/07/2026: nunca apagar. Errou de novo? Acrescente aqui na hora.

---

## 0.1. ⚠️ LEIA ANTES DE AFIRMAR QUALQUER COISA

> O Lucas pediu explicitamente que estes erros ficassem registrados para não se repetirem.
> Não apagar esta seção.

**Padrão observado em duas sessões: toda vez que afirmei sem medir, errei. Sem exceção.**
E toda vez que rodei o comando, o número contrariou minha estimativa.

| O que eu afirmei | O que a medição mostrou | Como eu deveria ter descoberto |
|---|---|---|
| "~153 KB de CSS duplicado" | 146,6 KB **no total**; duplicação real: 48,7 KB | contar, em vez de estimar por olho |
| "as tabelas de patente já divergiram" | **idênticas** — e usei isso para justificar a prioridade do Bloco A | `diff` das duas |
| "o furo é `verify_jwt = false`" | ligar o flag **não resolve**: o gateway aceita a chave pública | um POST na função |
| "redefinição de senha quebrada" | pior — **todo o provedor de e-mail estava desligado** | `GET /auth/v1/settings` |
| "chegou um e-mail do meu teste" | **não chegou**; quem avisou foi o Lucas | ler os logs |
| "eu quebrei a notificação de cadastro" | não quebrei; **concluí antes de olhar** | `get_logs` |
| "o Lucas não fez a Parte 2 do webhook" | ele fez; o cabeçalho estava lá, o **valor** é que diferia | `pg_get_triggerdef` |
| "senha vazada é toggle de custo zero" | **HTTP 402** — recurso do plano Pro | tentar aplicar |
| "não há bloqueio de força bruta" | há, **na 32ª tentativa** — fraco, mas existe | 45 tentativas reais |

**Por que isso é grave aqui:** o Lucas não é técnico e não tem como auditar o que eu digo.
Afirmação errada minha vira decisão errada dele. No caso das tabelas de patente, virou a
priorização de um bloco inteiro de trabalho.

**Regra de trabalho:**
1. Antes de afirmar número, estado de sistema ou causa de falha — **rodar o comando.**
2. Quando não der para medir, dizer explicitamente que é estimativa.
3. Ao registrar aqui, **guardar junto o comando que produziu o número**, para reconferir depois.
4. Diagnóstico de falha começa em `get_logs` / `execute_sql`, nunca em hipótese.
5. **Errou? Acrescente uma linha na tabela acima, na hora.** Ordem direta do Lucas em
   30/07/2026: *"sempre que você cometer um erro você irá adicionar uma linha de contexto no
   .md, para não cometer mais o mesmo erro"*. Ele trata erro como normal — o que não é normal
   é repetir.

| Erro | O que aprendi |
|---|---|
| Reescrevi 2 HTMLs com `Set-Content` do PowerShell e **destruí todos os acentos** (`Astral â€" Recursos`) | O PS 5.1 lê UTF-8 como ANSI. **Editar HTML deste projeto só com a ferramenta Edit ou com Node.** A regra já estava escrita na seção 2 — e eu não consultei antes de agir. Ter a regra no arquivo não basta se eu não a leio. |
| Escrevi o passo a passo do hCaptcha **de memória**: mandei procurar "Hostnames" (hoje é **Domains**) e a secret dentro do site (é **da conta**). O Lucas travou seguindo. | **Interface de site de terceiro muda — nunca descrever de memória.** Antes de escrever qualquer clique a clique, abrir a documentação oficial. Vale o mesmo que a regra 1 da seção 0.1: painel de terceiro é "estado de sistema", e eu afirmei sem medir. |
| Meu script de varredura esperava HTTP 400 num INSERT inválido e veio 401; **concluí que o formulário da landing estava quebrado** | Não estava. Validação que mora em **policy de RLS** volta como 401/42501, não 400 — o Postgres trata violação de policy como falta de permissão. Confirmei por fora antes de falar (INSERT válido → 201). Expectativa errada no teste vira falso alarme, que gasta a confiança do Lucas igual a um erro de verdade. |
| 🔴 **Derrubei o login em produção por ~2 min** ligando o captcha no servidor antes de publicar a sitekey — **seguindo a ordem que eu mesmo tinha escrito errada** na 13.5 | Instrução minha errada no `CLAUDE.md` é pior que instrução nenhuma: eu a sigo com confiança. **Ao escrever um procedimento de duas pontas, simular mentalmente as duas ordens e anotar o que quebra em cada uma.** A ordem certa é sempre: primeiro o lado que só *envia* a mais, depois o lado que passa a *exigir*. |
| Rodei **dois `git push` concorrentes** (um em background, um em foreground) e levei `cannot lock ref`; passei a achar que o push falhava | O primeiro tinha funcionado. **Nunca disparar dois pushes para o mesmo ref.** E ler o erro até o fim: ele dizia `is at f2502af`, que já era a resposta. |
| Consultei a produção **40 vezes em 4 minutos** para ver se o deploy saiu, e a Vercel me bloqueou (`X-Vercel-Mitigated: challenge`) — passei a achar que o deploy não tinha saído | Polling agressivo vira autossabotagem: eu criei o sintoma que fui diagnosticar. **Esperar 20–45s entre consultas** e usar `User-Agent` de navegador. |
| Escrevi `drop policy "qualquer um entra na lista de espera"` **com o nome de memória**. O nome real era outro, e o `if exists` transformou o erro num aviso silencioso — dei o trabalho por feito | **Nunca escrever `drop policy`/`drop index` com nome lembrado.** Consultar `pg_policies` antes. O `if exists` é uma faca: protege contra erro *e* esconde o engano. Salvou-me o `revoke` na mesma migration — duas barreiras existem para isso. |
| Meu teste de invasão deu "tudo bloqueado" (401 em tudo) e eu quase comemorei — **o teste nem estava autenticando**: misturei a chave `anon` antiga com a publishable nova, e depois o próprio captcha barrou o login do script | **Resultado negativo em teste de segurança não vale sem prova de que o ataque foi tentado com credencial válida.** O teste agora confirma que o token do atacante lê o *próprio* dado antes de tentar ler o alheio. |
| 🔴 Achei as habilidades trancadas atrás de 70% e **diagnostiquei o design deliberado como bug**. Escrevi no `CLAUDE.md`: *"recompensa que ninguém vê não é recompensa, é segredo"* — e a resposta dele foi **"é proposital"**, é conquista secreta, e descobrir é o pico de dopamina | **Inverti o produto inteiro.** A pesquisa que ele mandou fazer me contradiz na cara: *"surprise rewards often create stronger dopamine responses than expected ones"*. Não bastava eu ter medido o código — eu medi certo e **interpretei a intenção errado**. A seção 11 diz *"vale perguntar antes de preservar"*; eu não perguntei, concluí. **Código que parece errado pode estar certo: a pergunta é "por que fizeram assim?", não "quem quebrou isso?"** |
| Chamei as habilidades de **tag** e pus `RECRUTA` como estado da tag | *"Tag é tag, nível é nível"* — ordem direta dele. Recruta é **patente**, que sai do XP e muda conforme o edital. Tag é **especialidade**. Juntei dois sistemas que ele tinha separado de propósito, e quase construí em cima da confusão |
| Propus quest semanal que **expira no domingo** | *"a missão não some"*. Quest é desbloqueio **permanente**: cumpriu, ganhou, é seu. O que expira é liga/desafio semanal, que é outra coisa e ele não pediu |
| Deixei 3 interpolações sem escape em `edital.html` durante o Bloco B3 | Varri por **lista de nomes conhecidos** (`q.enunciado`, `m.nome`) e não por *origem do dado*. `estado.edital.*` não estava na minha lista mental. Por isso `tools/varre-xss.js` existe agora — a varredura não pode depender do que eu lembro. |
| Escrevi o frontmatter das 9 regras/skills com `description` contendo **dois-pontos no meio** (`Edge functions do Astral: autenticacao...`) | YAML puro **não aceita `: ` dentro de valor sem aspas** — 5 dos 9 arquivos não seriam lidos, e **em silêncio**: a regra simplesmente nunca carregaria e eu nunca saberia por quê. Peguei porque testei o parse em vez de olhar e achar bonito (0.36 #2). **Todo valor de frontmatter agora vai entre aspas, sempre.** |
| Consertei a pagina Minha Conta, publiquei, e **afirmei que estava resolvido**. O Lucas mandou print dela quebrada logo depois | O `vercel.json` guarda `/assets/*` por **1 hora** e a URL do CSS nao mudava quando o conteudo mudava. Producao estava certa; o **navegador dele** tinha o CSS velho. **"Publiquei" nao e sinonimo de "chegou no usuario"** — e eu tratei como se fosse. Pior: o efeito era PARCIAL (HTML novo + CSS velho), o que produz uma tela meio montada e parece bug de design. Corrigido com `tools/versiona-css.js`, que carimba o hash do conteudo na URL |
| **Tres vezes seguidas** eu unifiquei o HTML de uma peca compartilhada e **esqueci que o CSS dela tambem estava copiado**. Menu (02/08): unifiquei o HTML, conferi por hash que os 10 eram iguais, e o Lucas mandou print de dois menus com aparencias diferentes | Peca compartilhada tem DUAS metades: marcacao e estilo. Unificar so uma da a ilusao de resolvido -- e o hash do HTML **confirma** essa ilusao. **Ao unificar qualquer coisa, medir a APARENCIA RESOLVIDA, nao a fonte.** O teste certo e comparar `getComputedStyle` entre paginas; foi o que finalmente provou (1 aparencia distinta entre 10 paginas). Corrigido de vez removendo as 48 copias de `.nav-link` |
| Uma semana inteira de "erros bobos" — `$1$2` em 11 páginas, divs desbalanceadas, id duplicado, chave de CSS aberta, carimbo de versão só no CSS | **Não foi descuido pontual, foi FALTA DE REDE.** Eu edito 11 arquivos de uma vez com script; o script erra num detalhe; nada me avisa até o Lucas abrir a tela e reclamar. A causa estrutural, medida: **9.387 linhas duplicadas dentro das páginas contra 3.665 compartilhadas** — `.main` em 9 páginas, `.card` em 8, `.sidebar` em 8. Corrigido com `tools/verifica.js` (10 checagens, sai com código 1 e trava o commit) e 29 cópias de CSS unificadas. **Ferramenta que roda sozinha vale mais que promessa de ter cuidado.** |
| 🔴🔴 **O erro mais caro do projeto: TRÊS DIAS.** O nome do Lucas aparecia como "Luca". Eu procurei em CSS: largura, especificidade, ordem dos arquivos, cadeia de ancestrais, contenção, reticências. Reescrevi a regra em três arquivos, movi a divisa de lugar, apertei fonte e espaçamento. **Nada disso estava errado.** A causa era uma barra invertida perdida: `split(/s+/)` em vez de `split(/\s+/)` — `/s+/` é a **letra "s"**, então `"Lucas"` virava `"Luca"`, `"Alessandra"` virava `"Ale"` | **O sintoma era visual, a causa era o dado.** Texto cortado na tela não significa que o corte aconteceu na tela. **Regra que ficou: depois do PRIMEIRO conserto que não resolve, parar de mexer na apresentação e ir olhar o VALOR que chega.** Um `console.log` do texto teria fechado o caso em 30 segundos, no primeiro dia |
| Corolário do anterior, e é o que mais dói: **todas as minhas medições diziam "ok" enquanto o Lucas via quebrado.** Eu media a largura da caixa (cabia), a regra vencedora (correta), os ancestrais (não recortavam) — e concluía que estava resolvido | **Quando o teste diz "ok" e a pessoa diz "está errado", quem está errado é o teste.** Eu media a caixa; o problema era o conteúdo que entrava nela. Medição que não reproduz o defeito relatado não é prova de nada — é confirmação do que eu já queria acreditar. **Antes de dizer "resolvido", o teste precisa ter REPRODUZIDO o defeito pelo menos uma vez.** Se nunca reproduziu, não posso saber se consertei |
| **Três vezes no mesmo dia** a barra invertida sumiu por escrever código via `node -e` dentro de string de shell: `\s`→`s` (3 dias), `\d`→`d` (abreviação de patente nunca funcionava), `"$1$2"` literal (trocou o elemento do nome em 11 páginas) | O bash come a barra antes de o Node ver, e o resultado é **código válido que faz outra coisa**. Não quebra, não avisa, passa no teste. **REGRA ABSOLUTA: expressão regular e substituição com `$1` nunca se escrevem por `node -e` no shell — vão para arquivo, com o `Write`, e rodam de lá.** Virou a checagem 11 do `tools/verifica.js` |
| 🔴 **Rodei um comando em produção sem querer, escrevendo uma mensagem de commit.** Pus crase em volta de `supabase config push` dentro de `git commit -m "..."`. O bash trata crase em aspas duplas como **comando a executar**: ele rodou de verdade, sobrescreveu a configuração de autenticação da produção com valores de `localhost` (`site_url`, `uri_allow_list`, senha mínima 8→6) e enfiou a saída de erro dentro da mensagem do commit | **Texto que menciona comando não se escreve em `-m` com aspas duplas.** Vai para arquivo e entra por `git commit -F`. É a MESMA família do erro do `\s`: o shell interpretando o que era para ser texto. A diferença é que ali o estrago foi num arquivo, e aqui foi na **produção** — o retorno do login com Google, por onde entram 6 dos 8 usuários, ficou apontando para `127.0.0.1`. Detectado por acaso, ao ler a saída do push |
| Corolário: **o `checa-saude.js` disse "TUDO CERTO" com o login com Google quebrado.** Ele testa se a ida para o Google acontece (302), não se a **volta** é aceita | Teste de ida e volta precisa testar a volta. Nasceu daí o `tools/confere-auth.ps1`, que compara a configuração de autenticação da produção com o que ela deve ser, campo a campo, com o porquê de cada um |
| 🔴 **O primeiro erro meu que custou DINHEIRO.** Ao consertar o `pause_turn` da busca na web, escrevi um laço que **reenvia a conversa inteira a cada volta** — e ela já carrega os resultados da busca. Com 4 voltas, o mesmo conteúdo é cobrado 5 vezes na entrada. Somado às 8 tentativas que falhavam gastando ~70s de busca cada, **os créditos da Anthropic acabaram e a conta ficou em −US$ 0,96** | **Antes de escrever um laço que repete chamada paga, calcular quanto custa a repetição.** Eu tratei o `pause_turn` como problema de correção e não de custo. E o pior: **não havia medição de custo em lugar nenhum** — só a minha estimativa de planilha. Agora as duas funções gravam no log o consumo real de cada chamada, com o valor em dólar calculado dos tokens que a própria API informa |
| Corolário, e é sobre honestidade comigo mesmo: **eu concluí "os créditos acabaram", o Lucas disse um número, eu reinterpretei como "sobrou quase tudo" e me corrigi em voz alta — estando certo da primeira vez.** Ele estava em −US$ 0,96, não com US$ 0,26 gastos | **Não trocar uma conclusão medida por uma interpretação de frase ambígua.** Eu tinha a evidência (todas as chamadas falhando com erro de cobrança, inclusive uma função que funcionava minutos antes) e a abandonei por causa de uma leitura minha. Quando o dado e a frase discordam, perguntar — não escolher |
| 🔴🔴🔴 **08/09/2026 — O maior desperdício até hoje: auditei o PROJETO ERRADO, uma sessão inteira.** O diretório de trabalho era `Documents\ASTRAL`, uma cópia parada em julho. O projeto real estava em `Desktop\ASTRAL`. Produzi uma auditoria completa, 14 documentos, um roadmap de 70 etapas e duas correções de código — **tudo descrevendo código que não está em produção**. Afirmei que "não existe cronograma", "não existem termos de uso", "não há git", "não há rate limit", "o banco tem 7 tabelas órfãs". Existem `cronograma.html`, `termos.html`, 145 commits, a função `minha-quota`, e o código real usa as tabelas | **Antes de qualquer análise, PROVAR qual diretório é o projeto.** Os três sinais estavam na minha frente e eu passei por cima dos três: **sem `.git`**, **sem remote**, **sem `CLAUDE.md`** — um projeto com meses de trabalho não tem nenhum dos três só se for uma cópia. O que fecha a questão em 30 segundos é comparar com o que está **no ar**: `curl` na produção e `cmp` com o arquivo local. Fiz isso só depois de horas, e foi decisivo (`index.html` local == produção, byte a byte). **Nasce daqui a checagem 0 de toda sessão: `git remote -v` + existência de `CLAUDE.md` + um `cmp` contra produção.** E o agravante: eu tinha acabado de escrever um protocolo de auditoria com 8 regras sobre evidência, e nenhuma delas cobria "estou olhando o projeto certo?" — a regra mais básica de todas |
| 🔴 **17/09/2026 — Repeti o erro da crase, e a regra contra ele está escrita no CLAUDE.md desde 03/08.** Escrevi texto em português dentro de `node -e "..."` no shell, com `` `FUNCOES_DESLIGADAS` `` e `` `_shared/comum.ts` `` entre crases para virar código no Markdown. O bash **executou as duas** como comando e gravou o arquivo com dois buracos: *"(  em  )"* | A regra existente fala de **mensagem de commit**; eu li como se fosse só sobre commit. **Não é sobre commit — é sobre crase dentro de aspas duplas no bash, em QUALQUER comando.** Vale para `node -e`, `echo`, `curl`, tudo. **Texto em português com marcação vai para arquivo com o `Write` e roda de lá** — mesma conclusão da regra do `\s`, e agora pela terceira família de sintoma. O barato é escrever o script; o caro é descobrir depois que o arquivo saiu furado |
| 🔴🔴 **16/09/2026 — Deixei o site INTEIRO sem fonte, e só o teste pegou.** No CSS do Google o comentário `/* latin */` vem **antes** do `@font-face`. Dividi o arquivo por `"@font-face"` — e cada pedaço ficou com o comentário do bloco **seguinte**. Off-by-one. Os `unicode-range` foram para os arquivos errados, **nenhuma face cobriu o alfabeto latino** e todo o texto do Astral caiu na fonte do sistema | **Ao fatiar um arquivo por um delimitador, perguntar de que lado fica o rótulo.** Comentário que descreve o bloco seguinte é padrão comum (CSS do Google, changelogs, dumps SQL) e `split` sempre associa errado. **E: toda transformação em massa precisa de uma asserção que só passe se o resultado fizer sentido** — aqui, "toda face `latin` tem de conter `U+0000-00FF`". Sem ela eu teria publicado o site sem fonte. Com ela, o erro morreu em 1 minuto |
| **16/09/2026 — Removi o bloco errado do `base.css` e levei junto o cabeçalho original** — justamente o que avisa "este é o ÚNICO lugar onde cor, tipo, espaço e movimento são definidos" | Meu corte foi "do início até a primeira linha que eu reconheço" — e o que veio antes do meu bloco foi junto. **Para desfazer uma inserção, não cortar por posição: cortar pelas marcas que EU escrevi**, ou restaurar do `git` e reinserir. Restaurado do `HEAD` |
| 🔴 **16/09/2026 — Busca-e-substitui não distingue código de CONTEÚDO.** O script de cor do item 8 trocou hex por token também **dentro de `<code>`** no guia de estilo: a página que existe para **mostrar** `#0E1620` passou a exibir `var(--breu)`. 8 casos, e só apareceram um item depois, por acaso, enquanto eu investigava fontes | **Substituição em massa em HTML tem de saber onde está.** Dentro de `<style>` e de atributo é código; entre `>` e `<` é texto que alguém lê. A varredura de conferência que fiz depois — procurar `var(--` em texto visível nas 19 páginas — devia ter rodado **antes do commit**, não dois itens depois |
| 🔬 **16/09/2026 — Quase reportei 12 páginas quebradas por um teste com ruído maior que o sinal.** Comparei screenshot antes × depois e deu 12 páginas diferentes, delta até 210. Antes de concluir, rodei o CONTROLE — a mesma versão dos dois lados: **10 páginas diferentes, delta até 76** | **O site renderiza de forma não determinística**, então print-contra-print não prova nada aqui. **Toda prova visual roda o controle primeiro**: sem o piso de ruído medido, o resultado não tem significado. Métricas que se provaram limpas no controle: cor calculada normalizada em canvas (0) e largura de bloco de texto (0 de 1192). A do print: inútil. Detalhe e suspeitos em `rules/paginas.md` |
| 🔴 **15/09/2026 — Quatro vezes no mesmo dia, o INSTRUMENTO mentiu e eu quase acreditei.** (a) Meu servidor de teste devolvia **404 em tudo** — `path.join` usa `\` no Windows e meu `startsWith` comparava com uma raiz em `/`. Medi **página em branco** e concluí "sem rolagem horizontal, está ok". Peguei porque fui conferir o `<title>` e ele veio **vazio**. (b) No Playwright a rota registrada **por último** vence; meu curinga `**/rest/v1/**` engolia a rota específica de `perfis`, então o teste **nunca exercitou a falha** que eu dizia ter consertado. Peguei porque instrumentei `perfil pedido: sim/NAO`. (c) Numa terceira, a página tinha **redirecionado para o login** e eu media a tela errada achando que era o dashboard | **É o mesmo padrão de agosto — "todas as minhas medições diziam ok enquanto o Lucas via quebrado" — mas um nível abaixo: ali o teste media a coisa errada; aqui ele não media coisa nenhuma.** A regra que já existia ("teste que nunca reproduziu o defeito não prova conserto") precisa de um passo ANTES: **provar que o teste chegou no alvo.** Título da página, URL final, e um contador do tipo "a chamada que eu quero testar aconteceu?" — sem isso, "passou" pode significar "não rodou". Todo teste novo nasce com essa asserção |
| **15/09/2026 — `grep Inter` casou com `setInterval` 7 vezes.** Reportei "9 referências a fontes mortas"; eram **3**. As outras 7 eram `setInterval`/`clearInterval` | **Contagem por substring não é medição.** Já está na seção 0.1 que estimativa erra sempre — isto é pior, porque *parece* medição e vem com número. Conferir a **linha inteira** antes de reportar qualquer contagem de `grep`, ou usar `-w`/delimitador |
| **15/09/2026 — Python gravou `app.css` em CRLF num repositório LF.** 7 mudanças reais viraram um diff de **566 linhas**, impossível de revisar | `io.open(p,'w')` no Windows traduz `\n` em `\r\n` sozinho. **Depois de reescrever arquivo por script, conferir o diff ANTES de commitar** — se o número de linhas não bate com o número de mudanças, algo invisível entrou junto. Foi assim que o `Set-Content` destruiu acento em agosto: mesma família, instrumento mexendo no que não foi pedido |
| **15/09/2026 — `tools/valida-css.js` estava morto e ninguém sabia.** Tinha `c:/Users/Lucas/Desktop/ASTRAL` escrito à mão e morria com ENOENT desde que o projeto mudou de pasta | Não foi erro de escrita — foi **caminho absoluto num utilitário**. Ferramenta que só funciona num computador específico não é ferramenta. Raiz sai de `__dirname`, sempre. E vale a pergunta: **quantas outras ferramentas estão quebradas em silêncio?** Só se descobre rodando |
| Corolário do anterior: **`mv` falhou e meu script imprimiu "ok"**, porque eu tinha posto o `echo` de sucesso depois do comando sem checar o código de saída. O `mv` seguinte então **aninhou o projeto real dentro da cópia velha** (`Documents\ASTRAL\ASTRAL`) — comportamento normal do `mv` quando o destino já existe | **`set -e` não salva script que imprime sucesso por conta própria.** Comando que move ou apaga: capturar `$?` e ramificar, um passo por vez, com verificação entre eles. E o `mv` do Unix tem duas semânticas silenciosamente diferentes (renomear vs. mover-para-dentro) que dependem de o destino existir — **sempre testar `Test-Path` no destino antes**. Nada se perdeu porque `mv` move e não apaga, e o projeto tinha backup no GitHub; mas a recuperação custou 5 comandos que não precisavam existir |
| 🔴 **17/09/2026 — Duas páginas novas ficaram INUSÁVEIS no celular e passaram por 6 sessões sem ninguém ver.** `tags.html` e `cronograma.html` não tinham `.sidebar { transform: translateX(-100%) }` na consulta de 768px. A barra é `fixed`, 240px: no Android de 360px ela cobria **240 de 360 — dois terços da tela**, por cima do conteúdo, e o botão de menu "não abria" porque ela já estava aberta | **Uma correção feita "para as 9 páginas" não protege a 10ª.** A `paginas.md` §8.11 até dizia "as 9 páginas escondem a sidebar" — o número estava certo no dia em que foi escrito e envelheceu calado. Corrigido em 17/09 nas duas, e o `testa-celular.js` agora **mede** se a barra se esconde, em vez de confiar na contagem. Quem cria página do app copia de outra: a armadilha volta se o teste não existir |
| 🔬 **17/09/2026 — Meu próprio teste de celular deu 60 alvos de toque "pequenos demais"; **reais eram 1**, e esse 1 também era engano meu.** Eu media todo `<a>` e todo `<input>` com a régua dos 44px, sem distinguir o que é controle do que não é | **Alvo de toque tem três casos que a régua não cobre, e eu não tinha nenhum:** (1) link no meio de frase tem o tamanho da palavra — aumentá-lo estragaria o parágrafo; (2) caixa de marcação com `<label>` — o alvo real é o rótulo inteiro, não o quadradinho de 18px, e vale nas **duas** formas, `label[for=]` e `<label><input>texto</label>`; (3) botão de 207×37 é apertado mas ninguém erra — **a largura salva**. Falha só quando é pequeno nos **dois** lados. Depois de classificar: 60 → 0 falhas e 1 conserto de verdade (o olhinho da senha, 32×32) |
| 🔴 **17/09/2026 — Quebrei a regra 5 DUAS VEZES no MESMO DIA, por dois mecanismos diferentes.** De manha a crase (linha acima). A tarde, rodei `node -e` com aspas pelo bash para editar um script e o shell comeu as barras invertidas e `\f` e `\n` viraram caractere literal dentro do código. O arquivo passou a ter uma quebra de linha no meio de um comentário e parou de rodar | A regra diz, com todas as letras: **expressão regular e `$1` de replace NUNCA por `node -e` no shell — vai para arquivo com o `Write`, e roda de lá.** Eu a li nesta mesma sessão e usei `node -e` assim mesmo, porque era "uma edição pequena". **Não existe edição pequena o bastante para valer a exceção:** o custo de escrever o arquivo é 10 segundos, e o de descobrir o estrago foi maior nas tres vezes. **Duas ocorrencias no mesmo dia dizem que o problema nao e memoria, e sim que eu trato `node -e` como atalho aceitavel quando a edicao parece pequena.** Nao e: a regra nao tem excecao por tamanho |
| 🔴 **19/09/2026 — Escrevi na apresentação, na seção "JÁ ESTÁ NO AR", que as habilidades enferrujam em 7 dias e suspendem em 14. Fui mexer nisso hoje e descobri que o decaimento das habilidades NUNCA funcionou.** O código lia `materia.ultimoEstudo`, um campo **lido numa linha e escrito em nenhuma** do projeto inteiro. Sem ele, `diasSemEstudar` caía sempre em 999 e **TODA habilidade desbloqueada aparecia como suspensa** — inclusive a de quem tinha estudado cinco minutos antes | **O defeito não era meu; a AFIRMAÇÃO era.** Eu li a legenda da tela ("Enferrujada, 7 dias sem estudar"), li o código que calculava os três estados, e concluí que funcionava — sem nunca ter conferido se o campo que alimenta a conta existia. **Ler o código não é medir.** Duas coisas mudam daqui: (1) antes de escrever qualquer linha em "já está no ar", rodar; (2) quando um cálculo depende de um campo, procurar **quem grava** esse campo, não só quem lê. `grep` por um nome de campo que só aparece uma vez é sinal de alarme — dado que ninguém escreve é dado que não existe |
| 🔴 **19/09/2026 — TERCEIRA ferramenta, mesmo erro: o Python gravou `\b` como BACKSPACE (0x08) dentro de uma expressão regular do meu teste.** O teste então acusou **falha com a tela certa** — os números na tela eram exatamente os esperados, e `/\b64\b/` nunca casava porque o `\b` tinha virado caractere de controle invisível | A regra do `CLAUDE.md` fala em `node -e` **no shell**. Eram duas ferramentas; agora são três (bash, Python, e o próprio `node -e`). **O padrão não é a ferramenta — é passar expressão regular por QUALQUER camada que interprete barra invertida antes de chegar ao disco.** Duas lições: (1) escrever o arquivo com o `Write`, direto, sempre; (2) **quando der para comparar sem barra invertida, comparar sem barra invertida** — troquei o regex por comparação de lista e o problema deixou de poder existir. E um agravante: um teste que falha com o sistema CERTO é pior que teste nenhum, porque manda consertar o que não está quebrado |
| 🔴 **20/09/2026 — Acrescentei uma coluna a uma tabela com grant amplo e ela nasceu ESCREVIVEL pelo usuario.** A coluna era `xp_validado`, cuja razao de existir e ser escrita SO pelo servidor -- ordem dele: *"nao quero ninguem alterando isso a nao ser nos"*. Mas `progresso` tinha, desde 30/07, um `grant select, insert, update` de **tabela inteira** | **Grant de tabela vale para coluna que ainda nem existe.** Acrescentar coluna a uma tabela com grant amplo NAO e operacao neutra: o campo novo herda a permissao do mais permissivo que ja estava la, em silencio. O teste pegou em um minuto (um PATCH gravou 999999 e o banco aceitou), e o conserto foi trocar por **permissao coluna a coluna**. Daqui em diante: **coluna nova em tabela com grant amplo = conferir o grant no mesmo commit** |
| 🚨 **20/09/2026 — E ao consertar o buraco acima, QUEBREI O SALVAMENTO DE PROGRESSO em producao.** Troquei o grant de tabela por grant coluna a coluna e deixei `atualizado_em` de fora, raciocinando que quem escreve essa coluna e o gatilho. Esta errado: `salvar_progresso` escreve `atualizado_em = now()` explicitamente, e ela e `security invoker` -- roda com a permissao de quem chamou. **Ninguem conseguia salvar progresso** enquanto durou | **Eu raciocinei sobre INTENCAO em vez de LER O CODIGO** -- a mesma falha do `ultimoEstudo` de ontem, com outra roupa. Trocar grant de tabela por grant de coluna exige ler TODA funcao que escreve naquela tabela, uma por uma. E o agravante: **o comentario que eu mesmo escrevi na migration avisava** que perder uma coluna ali quebraria o salvamento -- escrevi o aviso e cai nele na mesma migration. O teste pegou em menos de um minuto, e foi so por isso que durou menos de um minuto |
| ⚠️ **20/09/2026 — Um script meu imprimiu "grupo acrescentado" sem ter acrescentado nada.** Fiz um `replace` em texto e mandei imprimir sucesso em seguida, sem conferir se o trecho tinha sido achado. A trava nova do `verifica.js` ficou meia hora existindo sem estar registrada -- ou seja, sem nunca aparecer no relatorio | **Corolario direto do `mv` que imprimiu "ok" em 15/09, e eu repeti.** Script que altera arquivo tem de **conferir que alterou**: se o trecho nao foi achado, falhar alto. Mensagem de sucesso escrita antes da conferencia e mentira automatizada -- e pior que silencio, porque para a investigacao |
| ⚠️ **17/09/2026 — Python gravou `\u2028` e `\u2029` literais dentro de um comentário JavaScript.** Eu tinha escrito os dois códigos no texto para *explicar* que o `^` casa depois deles. O Python os gravou como os caracteres de verdade — e **o JavaScript trata U+2028 e U+2029 como quebra de linha**, então o comentário partiu ao meio e o resto virou código inválido | **Nomear um caractere invisível dentro do código é escrevê-lo.** Quando um comentário precisa citar um caractere de controle, citar pelo **nome ou pelo código** (`U+2028`, `0x0C`), nunca o caractere. Mesma família do erro de `Set-Content` que destruiu os acentos: a ferramenta gravou literalmente o que eu quis dizer figurativamente |

| ⚠️ **20/09/2026 — O teste que devia me proteger tinha a lista de páginas ESCRITA À MÃO, e a página nova não entrou nela.** Criei `habilidades.html` e rodei o `testa-celular.js`: verde. Ele nunca abriu a página nova — a checagem do menu no celular percorria 12 nomes digitados dentro do próprio arquivo | **É a repetição exata do caso de 17/09 (`tags` e `cronograma`), uma camada acima.** Naquele dia a contagem envelheceu calada na *documentação*; agora envelheceu calada no *teste escrito para corrigir aquilo*. **Lista de arquivos nunca se escreve à mão — pergunta-se aos arquivos.** Agora ela sai de quem declara `class="sidebar"`: 12 nomes fixos viraram **13 páginas encontradas**, e a nova entrou sozinha. Generalização: **teste com inventário fixo protege o passado, não o próximo arquivo** — e é justamente o próximo que ninguém revisa |
| 🔴 **20/09/2026 — FOI ELE QUEM VIU, com um print: a barra lateral acendia a página errada.** Clicava em *Quadro* ou em *Instrução*, a página trocava, e quem ficava aceso em amarelo era *Minhas tags*. `arvore.html` e `habilidades.html` nasceram de uma cópia da barra de `tags.html` e o `active` veio junto — eu acrescentei o link novo e **não movi a marca** | **Passou por TUDO porque nada disso e defeito de funcionamento:** a página certa abre, o conteúdo certo aparece, nenhum erro é lançado, o `verifica` fica limpo, o teste de celular abre o menu. Só a **orientação** mente — e orientar é a única coisa que a barra lateral existe para fazer. **Dois aprendizados:** (1) **página nova copiada de outra herda o estado da outra**, e estado copiado não dá erro, só fica errado em silêncio; (2) eu só testo o que sei perguntar — **ele achou olhando, e eu não tinha nenhuma checagem que olhasse**. Virou a checagem 16 do `verifica.js`, que pergunta aos arquivos quem tem barra lateral e exige que o único `nav-link active` aponte para a própria página. Provada contra os **três** jeitos de errar: marca no link errado, nenhum aceso, e dois acesos |
| ⚠️ **20/09/2026 — Caminho absoluto desta máquina dentro de ferramenta, TERCEIRA ocorrência.** Achei um escondido no `testa-botoes.js` (`path.resolve("C:/Users/Lucas/Documents/ASTRAL")`) e **quase escrevi mais dois**, ao promover dois scripts do rascunho para `tools/` sem trocar a raiz | Em 15/09 o `valida-css.js` estava morto havia semanas pelo mesmo motivo. **O modo de falhar é o problema: a ferramenta não grita, ela some** — passa a não testar nada e o relatório continua bonito. Não adiantou a lição estar escrita: eu a reli em 15/09 e repeti em 20/09. **Então virou checagem 17 do `verifica.js`**, que varre `tools/` e falha se achar caminho desta máquina **em código** — ignorando comentário de linha **e de bloco**, porque na primeira versão ela acusou as próprias linhas que explicam o erro, e **alarme falso ensina a ignorar o alarme** |
| 🔴 **22/09/2026 — COMMITEI COM O `verifica.js` FALHANDO, e a rede de segurança não me segurou.** Rodei `node tools/verifica.js 2>&1 | tail -3 && git commit ...`. O `verifica` saiu com **código 1**, mas num **pipeline o código de saída é o do ÚLTIMO comando** — o `tail`, que sempre dá certo. O `&&` viu sucesso e commitou. O carimbo de versão de `prova.js` em `importar.html` estava defasado: quem abrisse a tela receberia **a versão velha do leitor** | **A regra 5 do CLAUDE.md diz "ANTES DE TODO COMMIT: verifica.js" — e eu rodei. O problema não foi esquecer, foi **rodar de um jeito que não podia falhar**.** Canalizar para `tail`, `grep` ou `head` **joga fora o código de saída** e transforma uma trava em enfeite. É da mesma família do `mv` que imprimiu "ok" em 15/09 e do script que disse "grupo acrescentado" sem acrescentar: **mensagem de sucesso desacoplada da conferência**. Daqui em diante, antes de commitar: rodar o verifica **sozinho**, guardar a saída num arquivo, e conferir `$?` — nunca `verifica | tail && commit` |
| 🔴 **23/09/2026 — QUARTA VEZ o mesmo defeito, e a pior das quatro: um `\b` de expressão regular foi gravado como o caractere BACKSPACE (0x08) de verdade.** O regex virou *"backspace seguido de Q"*, que nunca casa com nada. Eu estava ensinando o leitor a entender o gabarito do bombeiro do ES (`Q01 Q02 Q03`) e ele devolvia **zero respostas**, calado | **O modo de falhar é o que torna isto grave: não dá erro de sintaxe, não dá aviso, o código roda.** Ele só devolve vazio, para sempre. Gastei **oito comandos** procurando no lugar errado — reli a função três vezes, testei o regex isolado (funcionava), desconfiei de cache de módulo, de duplicata de função — e só achei quando rodei `od -c` na linha e vi os BYTES. **A lição nova não é "usar string crua", que eu já sabia: é que, quando o código está visivelmente certo e o resultado é vazio, o próximo passo é olhar os bytes, não reler a lógica.** As três anteriores estão acima (17/09 `\u2028`, 17/09 `\f` pelo shell, 19/09 `\b` num teste). Agora é a **checagem 18 do `verifica.js`**: nenhum caractere abaixo de 0x20, além da quebra de linha, pode existir em `.js` ou `.css` do projeto. Provada contra BACKSPACE e FORM FEED, e contra texto limpo para não dar alarme falso |
---
